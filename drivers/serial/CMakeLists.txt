LIST(APPEND SERIAL_DRIVER_SOURCES serial.c)
LIST(APPEND SERIAL_DRIVER_SOURCES kshell/kshell.c)

LIST(APPEND SERIAL_DRIVER_HEADERS serial.h)

ADD_LIBRARY(DriverSerial OBJECT ${SERIAL_DRIVER_HEADERS} ${SERIAL_DRIVER_SOURCES})
