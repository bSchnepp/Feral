INCLUDE_DIRECTORIES(../../)

IF (${FERAL_BUILD_STANDALONE_UEFI_})
LIST(APPEND KRNL_ENTRY_SOURCES efientry.c)
LIST(APPEND KRNL_ENTRY_SOURCES charmap_default.inl)
ELSE ()
LIST(APPEND KRNL_ENTRY_SOURCES mb2entry.c)
ENDIF()

ADD_LIBRARY(EntryLib OBJECT ${KRNL_ENTRY_SOURCES})
